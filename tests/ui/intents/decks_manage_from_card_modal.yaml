# Scenario: Add a card to a deck using the card modal controls
#
# Related:
#   issues: [127]
#   pull_requests: []

description: >
  I can assign an unassigned card to a deck from the card detail modal's
  Copies section. After assigning, the copy should show the deck name
  instead of "Unassigned".
