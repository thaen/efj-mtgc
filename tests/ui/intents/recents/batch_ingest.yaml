# Scenario: Select candidates and batch ingest into collection
#
# Related:
#   issues: []
#   pull_requests: []

description: >
  From the Recent Images page, I can select a candidate for an
  unresolved card, then batch ingest all resolved cards into the
  collection. After ingesting, the cards should appear on the
  Collection page when searched by name.
