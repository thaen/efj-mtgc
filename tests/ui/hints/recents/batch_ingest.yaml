start_page: /recent
involves:
  - time filter pills (30m, 2h, 4h, 12h, 24h)
  - card photo grid with red/green borders
  - accordion drawer opens when clicking a photo
  - candidate cards in the accordion with set icons and finish badges
  - '"Batch Ingest" button appears when at least one card is resolved (green border)'
notes: >
  The default 2h pill already shows all 5 images — no need to change it.
  Two cards (Mox Emerald, Armed Response) are already resolved (green
  border, DONE status). 1. Click "Batch Ingest" — it's visible because
  resolved cards exist. 2. A success message "N photos inserted" appears.
  3. Navigate to /collection. 4. Search for "Armed Response" to verify
  the card was added. Done.
