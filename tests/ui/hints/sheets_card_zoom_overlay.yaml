start_page: /sheets
involves:
  - "card elements .sheet-card (clickable)"
  - "zoom overlay #zoom-overlay (display:none by default, .active to show)"
  - "zoom image #zoom-img"
  - "section headers .section-header (click to expand)"
fixture_data:
  set: "blb"
  product: "play"
notes: >
  Load BLB play sheets via deep link. Expand a sheet section by clicking
  its header. Click the first .sheet-card to trigger showZoom(). Verify
  #zoom-overlay has .active class. Click #zoom-overlay to dismiss. Verify
  #zoom-overlay no longer has .active class.
